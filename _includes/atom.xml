<?xml version="1.0"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>SteveValaitis.com{%if page.title%} - {{page.title}}{%endif%}</title>
    <link href="{{page.feed_path}}/"/>
    <link type="application/atom+xml" rel="self" href="{{page.feed_path}}/atom.xml"/>
    <updated>{{ site.time | date_to_xmlschema }}</updated>
    <id>{{ site.url }}</id>
    <author>
        <name>{{ site.author.name }}</name>
        <email>{{ site.author.email }}</email>
    </author>

    {% for post in feed_posts %}
    <entry>
        <id>{{ site.url }}{{ post.id }}</id>
        <link type="text/html" rel="alternate" href="{{ site.url }}{{ post.url }}"/>
        <title>{{ post.title | xml_escape }}</title>
        <updated>{{ post.date | date_to_xmlschema }}</updated>
        <author>
            <name>{{ site.author.name }}</name>
            <uri>{{ site.url }}/</uri>
        </author>
        <content type="html">{{ post.content | mark_excerpt | xml_escape }}</content>
    </entry>
    {% endfor %}
</feed>

